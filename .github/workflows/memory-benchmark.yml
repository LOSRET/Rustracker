name: Memory Benchmark (Real Tracker)

on:
  workflow_dispatch:

jobs:
  benchmark:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - name: Build
        run: cargo build --release --example memory_tracker_bench

      - name: Run real-tracker memory benchmark
        run: cargo run --release --example memory_tracker_bench 2>bench.log | tee results.csv

      - name: Show summary
        run: cat bench.log

      - name: Render results table
        run: |
          echo "## Tracker RSS Growth Curve" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Label | Torrents | Peers | RSS | ΔRSS |" >> $GITHUB_STEP_SUMMARY
          echo "|---:|---:|---:|---:|---:|" >> $GITHUB_STEP_SUMMARY
          tail -n +2 results.csv | while IFS=',' read label torrents peers rss delta; do
            torrents_fmt=$(printf "%'d" $torrents)
            peers_fmt=$(printf "%'d" $peers)
            echo "| ${label} | ${torrents_fmt} | ${peers_fmt} | ${rss} MB | ${delta} MB |" >> $GITHUB_STEP_SUMMARY
          done

      - name: Check for memory jumps
        run: |
          echo "Checking for RSS jumps > 5 MB..."
          tail -n +2 results.csv | while IFS=',' read label _ _ _ delta; do
            delta_num=$(echo "$delta" | sed 's/ MB//')
            if [ "$(echo "$delta_num > 5" | bc -l 2>/dev/null || echo 0)" = "1" ]; then
              echo "⚠️  Jump: ${label} ΔRSS=${delta}"
            fi
          done

      - name: Upload results
        uses: actions/upload-artifact@v4
        with:
          name: tracker-memory-benchmark
          path: |
            results.csv
            bench.log
